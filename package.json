{
  "name": "vogt-ai",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:api": "pnpm --filter @vogt-ai/api dev",
    "dev:web": "pnpm --filter @vogt-ai/web dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
